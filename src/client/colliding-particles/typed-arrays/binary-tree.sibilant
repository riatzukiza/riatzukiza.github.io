(import Double-buffered-array "@colliding-particles/double-buffered.sibilant")
(import Binary-tree-node "@colliding-particles/data-types/binary-tree-node.sibilant")

(define Binary-tree Double-buffered-array
  ;; (element-type null)
  (data-type Binary-tree-node)

  ;; (cached data-type (.bind-type this.node-type this.element-type))
  (gett root (get this.data 0)))

(export Binary-tree)
