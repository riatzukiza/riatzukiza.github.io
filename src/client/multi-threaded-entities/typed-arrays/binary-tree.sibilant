(import Double-buffered-array "./double-buffered.sibilant")
(import Binary-tree-node "../data-types/binary-tree-node.sibilant")

(define Binary-tree Double-buffered-array
  (data-type Binary-tree-node)
  (gett root (get this.data 0))

  )

(export Binary-tree)
