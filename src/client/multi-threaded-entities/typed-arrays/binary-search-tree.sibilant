(define Binary-search-node Binary-tree-node
  (data-keys [])
  (index-key 'bid)

  (gett empty? (= -1 this.search-index))
  (gett occupied? (not this.empty?))

  (gett search-index (get this this.index-key))
  (gett keys [this.index-key ...this.data-keys])

  (def swap-keys (node)
    (.copy-to node this)
    (.copy-to this node)))

(define Binary-search-tree Binary-tree
  (def-generic insert (key (node this.root))
    (if)
    )
  (def find ())
  )
