
(require!           (lit Friction) "@shared/systems/physics/forces/friction.js"
          (lit game) "@crash-landed/game.js"
          config "@crash-landed/config.js"

          )

(define Friction Friction
  (config config)

  (template false)
  (def register () (assign this.config config))
  (def reset ())
  )


(export Friction)
